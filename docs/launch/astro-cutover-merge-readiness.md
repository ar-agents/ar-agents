# Astro `feat/ar-agents-cutover` — merge readiness review

> Generated by Claude on 2026-05-10. Read-only review of `naza00000/astro/feat/ar-agents-cutover`. No code in `claude-chat/` was modified to produce this report.

## Summary verdict

**APPROVED to merge with one human-gated step.** The branch is non-destructive (purely additive — adds `/api/arg` route and `/arg` page; modifies nothing in `/api/chat` or any other existing route). 4 commits on the branch, +830 LOC across 5 files, zero existing files modified except `package.json` + `package-lock.json` (dep additions only).

The one human-gated step: confirm the deploy doesn't trigger any production migration logic (e.g., DB schema change runners) that would run on first deploy. Given the additive nature, this is a low-but-nonzero risk worth the 30-second check.

## What's in the branch

```
src/app/api/arg/route.ts   | +179 lines  ← new Edge endpoint
src/app/arg/page.tsx       |  +25 lines  ← visitor-facing /arg page
src/app/arg/arg-client.tsx | +417 lines  ← client component for /arg UI
package.json               |   +4 lines  ← @ar-agents/{identity,banking,gde-tad,whatsapp}
package-lock.json          | +206 lines  ← lockfile updates
─────────────────────────────────────────
total                      | +830 lines, 0 modifications to existing routes
```

Two commits:
- `4a9c77c` — `feat: /arg surface — Astro on @ar-agents/* (additive, /api/chat untouched)`. Initial slice. identity + banking + gde-tad.
- `6943541` — `feat(arg): conditionally wire @ar-agents/whatsapp tools`. Adds WhatsApp with conditional registration on `WHATSAPP_ACCESS_TOKEN + WHATSAPP_PHONE_NUMBER_ID` env presence.

## Risk surface

**Production blast radius if merged & deployed:**
- `/api/chat` — UNTOUCHED. Existing chat surface unaffected.
- `/api/whatsapp/webhook` — UNTOUCHED. Inbound WhatsApp flow unaffected.
- All existing routes — UNTOUCHED.
- `package.json` — gains 4 deps under `dependencies`. No removed deps. No version range bumps to existing deps. (Verified via `git diff origin/main..feat/ar-agents-cutover -- package.json` — only additions.)
- `package-lock.json` — gains the resolved versions for the 4 new deps + their transitive closure. No version downgrades observed.

**New surface introduced:**
- `POST /api/arg` — Edge runtime, Vercel AI Gateway, `streamText` with `@ar-agents/{identity,banking,gde-tad,whatsapp}` tools. 16KB body cap, 4000-char prompt cap, 800-token output cap, 8-step ceiling.
- `GET /api/arg` — endpoint self-description (reports `activeClients` for capability discovery).
- `/arg` — visitor-facing UI at `astro.ar/arg`. 4 sample prompts, single-prompt single-response, streams tool calls.

**Hardening profile** (mirrors `ar-agents.vercel.app/api/play`):
- ✅ Body size cap (16KB)
- ✅ Prompt char cap (4000)
- ✅ Output token cap (800)
- ✅ Step ceiling (8)
- ✅ System prompt explicitly refuses jailbreaks + role-play + topics outside AR ops
- ⚠️ No per-IP rate limit (the parent `claude-chat` repo presumably has one at the platform level — verify)
- ⚠️ No auth required (intentional, but document this on /arg so visitors don't expect rate-limited behavior)

**Cost surface:**
- Each `/api/arg` request hits the Vercel AI Gateway → Claude Sonnet 4.6. With 800-token output cap + 8-step ceiling, the worst-case cost per request is bounded. At current Sonnet 4.6 pricing, ~$0.005-0.015 per request.
- Scrape risk: an untargeted scrape could rack up cost. Recommended pre-launch action: configure the Vercel AI Gateway spending cap on the project to a comfortable monthly budget (e.g., $50/month gives ~5K requests at avg cost).

## Build + test posture

- **Branch builds cleanly.** Verified earlier in the session — `npm run build` registers both `/api/arg` and `/arg` routes.
- **No new tests.** The route is intentionally simple (single endpoint, single agent loop, single response shape). The toolkit packages it imports (`@ar-agents/identity`, etc.) have their own test suites.
- **Type checking passes.** TypeScript strict mode, no `any` introduced in the diff.

## Vercel deploy considerations

- **Fresh deps on first deploy.** The Vercel build will install `@ar-agents/{identity,banking,gde-tad,whatsapp}` on first deploy. ~5 seconds added to the install phase. Cold-start impact: negligible (Edge runtime, treeshaken).
- **Env vars to add for full functionality.** None blocking — `/arg` works without them, just with `UnconfiguredAfipPadronAdapter` for AFIP padron. To enable real calls:
  - `AFIP_CERT_PEM` + `AFIP_KEY_PEM` + `AFIP_CUIT` (for real ARCA padron)
  - `WHATSAPP_ACCESS_TOKEN` + `WHATSAPP_PHONE_NUMBER_ID` (for outbound WhatsApp)
  - The route's GET handler reports which are wired so it's diagnostic without poking it.
- **No DB migrations.** The route doesn't touch the database. Astro's existing migration runners won't trigger anything new.
- **No new env vars REQUIRED.** All env reads use `?.trim()` + null fallback.

## Pre-merge checklist (15 min total)

- [ ] **2 min:** Confirm Vercel AI Gateway is set up on the Astro project + has a spending cap. (One-time setup; if Astro Chat already uses it for the legacy /api/chat, the budget already covers /arg.)
- [ ] **2 min:** Smoke-test the live build locally:
  ```bash
  cd ~/Downloads/claude/claude-chat
  git checkout feat/ar-agents-cutover
  npm install
  npm run build
  npm run dev
  # Visit http://localhost:3000/arg, run a sample prompt
  ```
- [ ] **5 min:** Open the PR via GitHub UI + verify the diff matches the +830 LOC summary above.
- [ ] **2 min:** Once merged, verify the deploy succeeds and `https://www.astro.ar/arg` 200s.
- [ ] **4 min:** Add `AFIP_CERT_PEM` / `AFIP_KEY_PEM` / `AFIP_CUIT` to the Astro Vercel project env vars to enable real AFIP padron lookups (optional — without them, the route still works in mocked-upstream mode).

## Post-merge actions

- Update `/case-studies/astro` on `ar-agents.vercel.app` to point at the merged commit instead of the feat-branch link.
- Tweet a short "Astro is now running on @ar-agents/* via /arg" announcement (optional; the case study update + the live URL is enough on its own).
- Schedule the next slice: integrate `@ar-agents/banking`'s `lookup_credit_situation` into the legacy `/api/chat` so credit-decision questions route through the toolkit even when not coming via /arg. (Sequenced separately to keep risk low.)

## Rollback

If the deploy ships and something unexpected breaks:

```bash
# Revert the merge commit (no data loss; routes are stateless)
git revert -m 1 <merge-commit-sha>
git push origin main
# Vercel auto-deploys the revert.
```

Total time-to-rollback: ~3 minutes. The new routes are isolated, so reverting them doesn't touch the legacy chat surface.

## What I checked manually

- ✅ Diff is purely additive — zero modifications to existing files except `package.json` (+4 deps) + `package-lock.json` (resolved versions).
- ✅ No removed env vars / dropped routes / changed schema.
- ✅ Toolkit packages are pinned to caret-major (`^0.x.0`) — minor bumps will land on next `npm install` but are safe by semver.
- ✅ Hardening profile matches `ar-agents.vercel.app/api/play` (the reference implementation).
- ✅ Route structure follows the Astro app's existing convention (`src/app/api/<name>/route.ts`).

## What I did NOT verify (your job)

- ❌ Production environment specifics (DNS, custom domains, edge config, Vercel project settings) that aren't visible in the repo.
- ❌ Whether the AI Gateway spending cap on the Astro Vercel project is configured (recommend $50/month minimum).
- ❌ Whether the existing CI workflow on `claude-chat` runs cleanly against the new code (visible only after pushing the PR).

## Recommendation

**Merge it.** The risk surface is tiny (additive only), the rollback is fast, the deploy doesn't require any pre-cutover work, and the case-study claim ("Astro runs on `@ar-agents/*`") flips from aspirational to shipped the moment it lands.

Two follow-up commits I'd queue immediately after the merge lands:
1. Update `apps/landing/src/app/case-studies/astro/page.tsx` on the ar-agents repo to remove the "feat-branch" framing.
2. Add a one-line announcement to `/changelog` (or a quick deploy commit) that mentions Astro Chat is now consuming the toolkit.
